# P2P System — Полное описание рабочей среды

**Дата составления:** 31 мая 2026 (обновлено 2 июня 2026)
**Состояние:** Production на VPS работает, локальная dev-среда настроена; Шаги 8А–8В часть 1 задеплоены

---

## 1. Что было сделано (краткая история)

До 30 мая 2026 проект `p2p-system` существовал только локально на ПК Acer Aspire XC600. Трекер собирал данные с Binance и Bybit, но только когда компьютер был включён. Из-за отключений света, рабочих смен и других причин трекер часто простаивал — данные собирались с дырами.

30 мая был куплен VPS у HostZealot (Варшава, Ubuntu 24.04). Туда переехали бэкенд (FastAPI) и трекер (APScheduler). С этого момента сбор данных идёт **круглосуточно и не зависит от состояния твоего ПК**.

Параллельно перед переездом были закрыты блокеры из code review:
- Защита write-эндпоинтов API-ключом (`X-Api-Key` header)
- CORS через переменные окружения
- Индексы в БД на ключевых таблицах
- Исправлен расчёт реализованного P&L
- Исправлен залипающий тултип в чарте
- Retry с дифференциацией HTTP-ошибок (429 / 5xx / network / 4xx)
- Создан `requirements_lock.txt` для повторяемых установок
- Созданы systemd unit-файлы

После переезда настроена новая локальная среда: ярлык `P2P` на рабочем столе, панель разработки с кнопками, скрипты для основных операций.

---

## 2. Архитектура

### 2.1. Production (VPS, Варшава)

```
┌─────────────────────────────────────────────────────┐
│  HostZealot KVM-SSD 1024  (Ubuntu 24.04)            │
│  IP: 185.223.57.171    SSH порт: 56777              │
│                                                     │
│  /opt/p2p-system/  (владелец: пользователь p2p)     │
│  ├── api/              FastAPI                      │
│  ├── modules/tracker/  APScheduler-сборщик          │
│  ├── core/             модели, БД, биржи, банки     │
│  ├── data/p2p.db       SQLite                       │
│  ├── venv/             Python 3.13                  │
│  └── config/.env       секреты (API_KEY и т.д.)     │
│                                                     │
│  systemd-сервисы:                                   │
│  ├── p2p-tracker.service   собирает данные каждые   │
│  │                          60 секунд               │
│  └── p2p-api.service       uvicorn на 0.0.0.0:8000  │
│                                                     │
│  Файрвол ufw:                                       │
│  ├── 56777/tcp  SSH                                 │
│  └── 8000/tcp   API                                 │
└─────────────────────────────────────────────────────┘
              ↑
              │ HTTP (X-Api-Key для POST/DELETE)
              │
┌─────────────────────────────────────────────────────┐
│  Локальный ПК (Acer Aspire XC600, Windows 10)       │
│                                                     │
│  C:\Users\inkvi\p2p-system\                         │
│  ├── frontend/         React + Vite (порт 5173)     │
│  ├── panel.html        твоя dev-панель              │
│  ├── start.bat         главный ярлык                │
│  └── *.bat             команды (dev, deploy и т.д.) │
└─────────────────────────────────────────────────────┘
```

### 2.2. Поток данных

1. **Сбор:** `p2p-tracker` на VPS делает HTTP-запросы к Binance/Bybit P2P API каждые 60 секунд, парсит ответы и записывает снапшоты в `data/p2p.db`.
2. **Отдача:** `p2p-api` (FastAPI) читает из той же БД и отдаёт данные по REST.
3. **Фронт:** локальный Vite на твоём ПК (`localhost:5173`) делает запросы к VPS API. GET-запросы открытые, POST/DELETE требуют `X-Api-Key` header.

---

## 3. Учётные данные

Все секреты живут локально в файлах **не попадающих в git**:

| Где хранится | Что там |
|---|---|
| `panel.html` (на ПК, в `.gitignore`) | Полная шпаргалка: SSH, GitHub, API-ключи |
| `vps_credentials.txt` (где ты сам положил) | Пароль root, пароль VPS-панели, пароль HostZealot аккаунта |
| `config/.env` (на ПК, в `.gitignore`) | DATABASE_URL, API_KEY, CORS_ORIGINS для локального бэка |
| `frontend/.env` (на ПК, в `.gitignore`) | VITE_API_URL, VITE_API_KEY |
| `/opt/p2p-system/config/.env` (на VPS) | то же что выше, production-значения |

**Что публично доступно (в git, на GitHub):**
- `config/.env.example` — шаблон с placeholder
- `frontend/.env.example` — шаблон с placeholder
- `panel.template.html` — версия панели без секретов
- `deploy/systemd/*.service` — unit-файлы (там нет секретов)
- Весь остальной код

---

## 4. Рабочий процесс — ежедневный workflow

### 4.1. Утром / придя с работы

1. Включаешь ПК
2. Двойной клик на ярлык **P2P** на рабочем столе
3. Открывается:
   - Окно "P2P Vite" — фронт собирается, через 5-10 секунд готов
   - Окно "P2P Panel" — Python HTTP-сервер для отдачи `panel.html` на порту 9999
   - Вкладка браузера с фронтом (`http://localhost:5173`)
   - Вкладка браузера с панелью (`http://localhost:9999/panel.html`)
4. На фронте смотришь графики, добавляешь сделки. Данные приходят с VPS.

### 4.2. Перед выключением ПК

Просто выключаешь компьютер. Никакие сервисы останавливать не нужно — основные сервисы крутятся в Варшаве. Окна Vite и Panel закроются сами, на VPS это никак не повлияет.

### 4.3. Если нужно посмотреть что на сервере

Открой `panel.html` (или используй вкладку панели). Там есть кнопки и команды:
- **SSH → VPS** — копирует команду подключения, вставь в PowerShell
- **Команды для VPS** — готовые команды для копирования: статус сервисов, логи трекера, логи API, рестарт, SQL-запросы к БД

---

## 5. Рабочий процесс — разработка

### 5.1. Принцип "две среды"

Чтобы случайно не сломать production, у тебя есть **изолированная локальная среда**.

**Локальная среда (на ПК):**
- Локальный FastAPI на `http://localhost:8000`
- Локальный фронт на `http://localhost:5173` ходит на локальный API
- Локальная SQLite БД (`data/p2p.db` на твоём ПК)
- Трекер локально **не запускается** — данных в локальной БД мало или нет
- Ломай здесь что угодно — VPS никак не пострадает

**Production среда (на VPS):**
- FastAPI на `http://185.223.57.171:8000`
- Трекер постоянно собирает данные в `data/p2p.db` на сервере
- Эту среду напрямую руками **не трогаешь**, только через `deploy.bat`

### 5.2. Переключение между средами

На панели разработки две большие кнопки:

- **Dev Local** → запускает локальный API + Vite, который смотрит на локальный API. Создаёт `frontend/.env.local` с `VITE_API_URL=http://localhost:8000`.
- **View Production** → запускает только Vite, который смотрит на VPS. Удаляет `frontend/.env.local`, тогда Vite использует `frontend/.env` где production-URL.

Переключение — это просто запуск нужного `.bat`. Под капотом меняется один файл.

### 5.3. Цикл разработки новой фичи

Допустим хочешь добавить новый эндпоинт или поправить фронт:

1. **Запускаешь `Dev Local`** — работаешь на локальной среде
2. **Меняешь код** в Claude Code или редакторе
3. **Тестируешь локально** — Vite с hot-reload, FastAPI с `--reload`
4. **Закоммитил изменения:** `git add . && git commit -m "..."` (через Claude Code или вручную)
5. **`Deploy → VPS`** на панели — код летит на сервер автоматически
6. **Проверяешь** что на VPS всё ок — кнопка View Production или SSH

### 5.4. Если нужны реальные данные для тестов

На локальной БД данных может не быть. Чтобы поиграться с реальными:

- Кнопка **Скачать БД** на панели → запустит `pull_db_from_vps.bat`
- Скопирует production-БД с VPS в `data/p2p_prod_2026-05-31_1530.db` (с timestamp)
- **НЕ перезаписывает** твою рабочую `data/p2p.db` — это отдельный файл для анализа
- Можешь временно подменить локальную БД на эту копию, если нужно

---

## 6. Описание всех файлов рабочей среды

### 6.1. На рабочем столе

| Файл | Что делает |
|---|---|
| Ярлык **P2P** | Запускает `start.bat` из корня проекта |

### 6.2. В корне проекта (`C:\Users\inkvi\p2p-system\`)

#### Точки входа

| Файл | Что делает |
|---|---|
| `start.bat` | Запускает Vite, поднимает HTTP-сервер для панели на порту 9999, открывает 2 вкладки в браузере |
| `panel.html` | Твоя dev-панель с кнопками и шпаргалкой. **Содержит секреты, в git не идёт.** |
| `panel.template.html` | Публичная версия панели без секретов. В git идёт. |

#### Скрипты управления (вызываются кнопками панели или вручную)

| Скрипт | Что делает |
|---|---|
| `dev_local.bat` | Локальная среда: API на 8000, Vite на 5173, оба смотрят на localhost. Создаёт `frontend/.env.local`. |
| `view_prod.bat` | Только Vite на 5173, смотрит на VPS API. Удаляет `frontend/.env.local`. |
| `ssh_to_vps.bat` | Открывает SSH-консоль к VPS. |
| `deploy.bat` | Закачивает `scripts/deploy_apply.sh` на VPS через scp, делает `git push`, потом через SSH запускает скрипт на сервере (pull + при необходимости pip install + restart). |
| `pull_db_from_vps.bat` | Скачивает копию production-БД с VPS в `data/p2p_prod_YYYY-MM-DD_HHmm.db`. |

#### Серверные скрипты (живут в репо, выполняются на VPS)

| Файл | Что делает |
|---|---|
| `scripts/deploy_apply.sh` | Bash-скрипт, который выполняется на VPS при деплое. Делает `git pull`, проверяет изменения в requirements, при необходимости `pip install`, затем `systemctl restart`. |

#### Архив старых скриптов (не запускать)

| Файл | Что это |
|---|---|
| `start_all_OLD_LOCAL.bat.bak` | Старый запуск всего стека локально. Больше не нужен. |
| `scripts/start_tracker_OLD_LOCAL.bat.bak` | Запуск локального трекера. Не нужен — трекер живёт на VPS. |
| `scripts/run_api_OLD_LOCAL.bat.bak` | Запуск локального API. Не нужен для production-просмотра. |
| `scripts/start_tracker_background_OLD_LOCAL.ps1.bak` | PowerShell-обёртка трекера с хардкодом путей. Не нужен. |

#### Сохранённые утилиты (можно использовать при необходимости)

| Файл | Что это |
|---|---|
| `scripts/stop_tracker.ps1` | Убивает локальные python-процессы трекера. Полезно если случайно запустил локальный трекер. |
| `scripts/3_MARKET.bat` | Однократно показывает market.py summary. |

---

## 7. Описание systemd-сервисов на VPS

### 7.1. p2p-tracker.service

**Расположение на VPS:** `/etc/systemd/system/p2p-tracker.service`
**Исходник в репо:** `deploy/systemd/p2p-tracker.service`

Запускает `scripts/run_tracker.py` от имени пользователя `p2p`. При падении автоматически перезапускается через 10 секунд (`Restart=always`).

Команды управления:
```bash
systemctl status p2p-tracker          # текущий статус
systemctl restart p2p-tracker         # перезапуск
systemctl stop p2p-tracker            # остановить
systemctl start p2p-tracker           # запустить
journalctl -u p2p-tracker -f          # смотреть логи в реальном времени
journalctl -u p2p-tracker -n 100      # последние 100 строк логов
```

### 7.2. p2p-api.service

**Расположение:** `/etc/systemd/system/p2p-api.service`
**Исходник:** `deploy/systemd/p2p-api.service`

Запускает `uvicorn api.main:app --host 0.0.0.0 --port 8000`. Сейчас слушает на всех интерфейсах (доступен снаружи) — это для текущего MVP. В будущем при добавлении Cloudflare Tunnel можно переключить на `127.0.0.1` (альтернативная команда есть в файле, закомментирована).

Команды управления — те же что у трекера, замени `p2p-tracker` на `p2p-api`.

---

## 8. Базовые команды на VPS

После подключения через `ssh_to_vps.bat` ты в shell на сервере. Полезные команды:

### Проверка работоспособности

```bash
systemctl status p2p-tracker p2p-api    # оба сервиса должны быть active (running)
curl http://localhost:8000/api/info     # должен вернуть JSON
```

### Просмотр данных в БД

```bash
sqlite3 /opt/p2p-system/data/p2p.db "SELECT COUNT(*) FROM snapshots;"
sqlite3 /opt/p2p-system/data/p2p.db "SELECT COUNT(*) FROM orders;"
sqlite3 /opt/p2p-system/data/p2p.db "SELECT MAX(collected_at) FROM snapshots;"
```

### Логи

```bash
journalctl -u p2p-tracker -n 50 --no-pager    # последние 50 строк
journalctl -u p2p-tracker -f                  # в реальном времени, Ctrl+C для выхода
journalctl -u p2p-tracker --since "1 hour ago" # за последний час
```

### Системные ресурсы

```bash
df -h                # сколько диска занято
du -sh /opt/p2p-system/data/p2p.db    # размер БД
free -h              # сколько памяти свободно
top                  # процессы (q для выхода)
```

### Файрвол

```bash
ufw status           # какие порты открыты
```

---

## 9. Известные нюансы и подводные камни

### 9.1. CORS на сервере

Сейчас на VPS в `config/.env` стоит `CORS_ORIGINS=*` (разрешены любые источники). Это упрощение, которое сделал автодеплой при первой настройке. Безопаснее ограничить конкретными origins (например `http://localhost:5173,http://127.0.0.1:5173`), но **не критично**: API-key защищает write-эндпоинты, а GET-запросы возвращают публичные данные о рынке.

Когда захочешь усилить — отредактируй `/opt/p2p-system/config/.env` через SSH, потом `systemctl restart p2p-api`.

### 9.2. API_KEY засветился в чате

В процессе настройки production API_KEY несколько раз попадал в наш диалог. Ты сознательно решил не ротировать его сейчас (никаких реальных денег и приватных данных в проекте нет — только котировки с публичных бирж). Когда соберёшься делать аудит безопасности — сгенерируй новый ключ и обнови в трёх местах:
1. `/opt/p2p-system/config/.env` на VPS
2. `frontend/.env` на ПК
3. `panel.html` на ПК
Потом `systemctl restart p2p-api` на VPS и `dev_local.bat` рестарт.

### 9.3. Vite иногда плавает на порт 5174

Если запустить два Vite одновременно, второй возьмёт порт 5174. Открывай в браузере `localhost:5173` или `5174` в зависимости от того, какое окно последнее живо. Перед запуском убедись что старый Vite закрыт (Ctrl+C в его окне или просто закрыть окно).

### 9.4. Provider SSH keys на VPS

В `/root/.ssh/authorized_keys` на VPS лежат SSH-ключи сотрудников HostZealot (Emergency Remote Hands). Это легитимная функция — провайдер может зайти на сервер чтобы помочь при критических проблемах. Если когда-то решишь это убрать — открой тикет в support и попроси удалить, либо удали руками (но тогда лишишься Emergency Hands поддержки).

### 9.5. Локальный трекер дублирует production

Если случайно запустишь старый `start_all_OLD_LOCAL.bat.bak` (или похожий) — на ПК поднимется второй трекер, который начнёт писать в **локальную БД**. Это не сломает production, но забьёт твой диск и создаст путаницу. Если такое случилось — `Ctrl+C` в окне трекера, окно закрыть.

### 9.6. БД не мигрирует автоматически

`deploy.bat` обновляет код и зависимости, но **не делает миграций БД**. Если когда-то добавишь новую колонку в `models.py` или новую таблицу — после деплоя сервис может упасть из-за несоответствия схемы. Решение: либо вручную запустить миграционный скрипт через SSH, либо подключить Alembic в будущем.

---

## 10. Что осталось сделать (не блокеры, на будущее)

Из исходного code review остались косметические улучшения:

- Поправить `CORS_ORIGINS=*` на конкретные origins (см. п. 9.1)
- Удалить неиспользуемые импорты в `api/routers/trades.py` и `api/services/market_service.py`
- Убрать debug `__main__` блоки из `core/exchanges/binance.py` и `core/exchanges/bybit.py`
- Вынести константы opportunities (`_TARGET_PROFIT_PER_USDT`, `_ENTRY_DISCOUNT_PERCENT`) из `market_service.py` в `settings.py`
- Удалить дублирование расчёта позиции между `market_service.py` и `position.py`
- Bybit-пагинация (сейчас фиксированно `page=1`) — если планируешь продолжать использовать Bybit

Эти задачи **не срочные**. Production работает корректно, данные собираются.

---

## 11. План развития

### Текущее состояние (на 2 июня 2026)

#### Сделано

- **8А:** таймфрейм-агрегация + обе линии BUY/SELL + перцентили — в проде на VPS
- **8Б:** PriceChart.tsx переписан на lightweight-charts v5 (две линии, сглаживание, таймфреймы, ResizeObserver)
- **Удаление сделок:** кнопка корзины + диалог подтверждения в блоке История
- **8В часть 1:** фильтр объёма `volume_uah` на графике (работает на VPS), тумблеры слоёв (Сделки/Коридор/Возможности — последние два задизаблены, каркас готов), метки сделок через `createSeriesMarkers` v5 с тултипом, сглаживание `LineType.Curved`

#### Хвосты

- Метки сделок не проверены вживую — журнал сейчас пуст. Проверить при первой внесённой сделке: появляется ли стрелка на нужном таймфрейме с нужной ценой.

---

### Следующее — 8В часть 2

1. **Полоса-коридор 25/75** — заливка между buy_p25 / buy_p75 и sell_p25 / sell_p75 (не линии). Тумблер «Коридор» уже есть в UI, нужно подключить слой.
2. **Слой opportunities** — точки выгодных ордеров на правом краю графика, тумблер «Возможности».
3. **Тултипы при наведении** — цена/объём/банк/дата по Киеву. Механизм `hoveredObjectId` готов.

> **⚠ Важно про фильтр объёма:** высокий `volume_uah` = меньше ордеров = **больше шума**, не меньше. Фильтр объёма — это «рынок доступный для моих сумм», а не инструмент сглаживания. Гладкость обеспечивает сглаживание линии (`Curved`) + коридор 25/75.

---

### Торговые сессии (крупная фича, 2–3 захода)

- Поле «Стартовый капитал ₴» + кнопка «Старт сессии»
- Привязка сделок к активной сессии, шапка «Сессия №N от … активна»
- «Закрыть сессию» + отчёт: реализовано / нереализовано (остаток USDT × текущий SELL-курс) / итого, BUY/SELL, оборот, длительность
- Журнал группируется по сессиям + общий учёт
- БД: новая таблица `sessions` + поле `session_id` в `trades`

---

### Отложенная инфраструктура

- **Telegram-алерты** — пороговые уведомления по цене/спреду
- **Миграция SQLite → PostgreSQL** — нужна при росте нагрузки
- **snapshots_hourly** — агрегированные снапшоты против тормозов на длинных ТФ при большой истории

---

### Шаг 9 — AI-агент (финал)

- Старт с простого: аналитик-комментатор (позиция + рыночная ситуация → Anthropic API → текстовый разбор)
- Потом: агент с tool use (может сам запрашивать эндпоинты)
- Осторожно с проактивными советами и финансовыми рекомендациями

---

Перед любой разработкой — переключайся на `Dev Local`, чтобы изолировать эксперименты от production.

---

## 12. Куда обращаться при проблемах

### Сервер недоступен (не открывается фронт)

1. Открой `ssh_to_vps.bat`
2. `systemctl status p2p-tracker p2p-api`
3. Если что-то не `active (running)` → `systemctl restart p2p-tracker p2p-api`
4. Если ребут не помог → `journalctl -u p2p-api -n 100` посмотреть ошибки

### Трекер не собирает данные

1. SSH на VPS
2. `journalctl -u p2p-tracker -f` — смотри живые логи
3. Если ошибки про Binance/Bybit API → возможно у бирж проблемы или rate limit, подожди
4. Если ошибки про БД → проверь место на диске `df -h`

### Фронт не открывается локально

1. Проверь что Vite реально запущен — должно быть окно "P2P Vite"
2. Открой `localhost:5173` (или 5174 если 5173 был занят)
3. Если Vite упал — закрой все окна, запусти заново через ярлык P2P

### Деплой упал

1. Открой `ssh_to_vps.bat`
2. `cd /opt/p2p-system && sudo -u p2p git status` — посмотри состояние репо
3. Если конфликты — разрулить вручную через git
4. Перезапусти `deploy.bat` после фикса

### Что-то совсем непонятное

Заходи в Claude Code или новый чат с Claude в браузере, описывай проблему, давай этот документ как контекст. Большинство проблем решаемы за 10-15 минут.

---

## 13. Контактные точки

| Сервис | URL/команда |
|---|---|
| Хостинг панель | https://my.hostzealot.com |
| Управление VPS | https://vpsmgr.yourbestnetwork.net |
| GitHub репо | https://github.com/waleronfm-ai/p2p-system |
| Production API | http://185.223.57.171:8000 |
| Swagger API docs | http://185.223.57.171:8000/docs |
| Локальный фронт | http://localhost:5173 |
| Локальная панель | http://localhost:9999/panel.html |

---

**Сохрани этот документ рядом с проектом** (например `C:\Users\inkvi\p2p-system\PROJECT_HANDOVER.md`). Когда зайдёшь в новый чат с Claude — приложи этот файл, и Claude мгновенно поймёт всю архитектуру и состояние проекта.

Удачи, Валерий. У тебя теперь настоящий production-сервис в облаке.
